1) data.json acts as the persistent data store and path in code is hardcoded to d:/data.json
2) AJAX working only in server environment.
3) Message ID ( MID ) acts as a primary key to seperate messages. Users have to enter it per message and it is not autogenerated.
4) WebContent/index.html is the landing page.
5) REST is used

