Can be done with settimeout() as when function is called rescursively a replicate copy of memory is 
created of the function due to which overflow occurs .If we use the settimeout ,function will be stored in 
queue so no replica of function is created in memory.   